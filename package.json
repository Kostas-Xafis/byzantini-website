{
	"name": "astro-test",
	"type": "module",
	"version": "0.0.1",
	"scripts": {
		"test": "bun test --env-file='./tests/.env.test' --timeout=10000",
		"typecheck": "tsc --noEmit",
		"astro-check": "bunx --bun astro check",
		"check": "bun run typecheck && bun run test",
		"format": "prettier --write src lib types getData tests",
		"format:check": "prettier --check src lib types getData tests",
		"loc": "find lib src getData types tests services email -path '*/node_modules' -prune -o -path '*/.git' -prune -o -path '*/.tmp' -prune -o -path '*/.bun-cache' -prune -o -type f \\( -name '*.ts' -o -name '*.tsx' -o -name '*.astro' -o -name '*.mjs' -o -name '*.css' \\) -print0 | xargs -0 wc -l | tail -1",
		"db:query": "bun ./getData/query.ts --dev --q",
		"db:query:prod": "bun ./getData/query.ts --prod --q",
		"db:replicate": "bun --env-file='.dev.vars.development' ./getData/replicate.ts",
		"db:reset": "rm -f dbSnapshots/latest.db dbSnapshots/latest.db-wal dbSnapshots/latest.db-shm && sqlite3 dbSnapshots/latest.db < dbSnapshots/dev-snapshot.sql",
		"db:logs": "bun run ./getData/query.ts --dev --q \"SELECT * FROM (SELECT query, args, DATE(date / 1000, 'unixepoch') as date, error FROM query_logs WHERE DATE(date / 1000, 'unixepoch') > '2025-10-14' ORDER BY date DESC LIMIT 100) ORDER BY date\"",
		"dev": "bunx --bun astro dev",
		"start": "CLOUDFLARE_ENV=development astro dev",
		"build": "CLOUDFLARE_ENV=production astro build",
		"bun-build": "CLOUDFLARE_ENV=production bunx --bun astro build",
		"preview": "CLOUDFLARE_ENV=production wrangler pages dev dist",
		"build-preview": "bun run bun-build && bun run preview",
		"deploy:test": "bun run build && bunx wrangler pages deploy dist --project-name byzantini-website --branch local-test",
		"logs:test": "bunx wrangler pages deployment tail $(bunx wrangler pages deployment list --project-name byzantini-website --environment preview --json | bun -e 'const text = await new Response(Bun.stdin.stream()).text(); const deployments = JSON.parse(text); const deployment = deployments.find((d) => d.Branch === \"local-test\"); if (!deployment) { console.error(\"No preview deployment found for branch local-test\"); process.exit(1); } process.stdout.write(deployment.Id);') --project-name byzantini-website",
		"docker:pdf": "sudo docker build -f ./services/pdfWorker/local.Dockerfile -t pdfworker ./services/pdfWorker/ && sudo docker run --network host -d pdfworker",
		"docker:img": "sudo docker build -f ./services/imageCompression/local.Dockerfile -t imgcomp ./services/imageCompression/ && sudo docker run --network host -d imgcomp",
		"docker:build": "sudo docker build -f ./services/pdfWorker/local.Dockerfile -t pdfworker ./services/pdfWorker/ && sudo docker build -f ./services/imageCompression/local.Dockerfile -t imgcomp ./services/imageCompression/",
		"docker:run": "sudo docker run --network host -d pdfworker && sudo docker run --network host -d imgcomp",
		"docker:logs": "sudo docker logs -f $(sudo docker ps -q --filter ancestor=pdfworker) && sudo docker logs -f $(sudo docker ps -q --filter ancestor=imgcomp)"
	},
	"dependencies": {
		"@astrojs/cloudflare": "^12.6.13",
		"@astrojs/sitemap": "^3.7.1",
		"@astrojs/solid-js": "^5.1.3",
		"@astrojs/tailwind": "^6.0.2",
		"@libsql/client": "^0.17.2",
		"@solidjs/router": "^0.15.4",
		"@tailwindcss/container-queries": "^0.1.1",
		"air-datepicker": "^3.6.0",
		"arctic": "^3.7.0",
		"astro": "5.18.0",
		"astro-seo": "^1.1.0",
		"fast-xml-parser": "^4.5.4",
		"solid-js": "^1.9.11",
		"tailwindcss": "^3.4.19",
		"valibot": "^0.13.1",
		"wrangler": "^4.76.0"
	},
	"devDependencies": {
		"@astrojs/check": "^0.9.10",
		"@aws-sdk/client-s3": "^3.1014.0",
		"@cloudflare/workers-types": "^4.20260317.1",
		"@types/bun": "^1.3.14",
		"client-zip": "^2.5.0",
		"pdfjs-dist": "^4.10.38",
		"prettier": "^3.9.6",
		"prettier-plugin-astro": "^0.14.1",
		"print-js": "^1.6.0",
		"ts-morph": "^24.0.0",
		"typescript": "6.0.3",
		"xlsx": "^0.18.5"
	}
}
